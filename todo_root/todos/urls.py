from django.urls import path
from . import views

app_name = 'todos'
urlpatterns = [
    # path('', views.IndexView.as_view(), name='index'),
    # path('new/', views.FormView.as_view(), name='new'),
    # path('<int:pk>/', views.DetailView.as_view(), name='detail'),
]
